name: Deploy Django App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          cd ~/vibetalk/
          ~/vibetalk/venv/bin/python -m pip install --upgrade pip
          ~/vibetalk/venv/bin/python -m pip install -r requirements.txt

      - name: Run Django Migrations
        run: |
          cd ~/vibetalk/
          ~/vibetalk/venv/bin/python manage.py makemigrations
          ~/vibetalk/venv/bin/python manage.py migrate

      - name: Collect static files
        run: |
          cd ~/vibetalk/
          ~/vibetalk/venv/bin/python manage.py collectstatic --noinput
      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd ~/vibetalk/
            git pull origin main
            ~/vibetalk/venv/bin/activate
            ~/vibetalk/venv/bin/pip install -r requirements.txt
            ~/vibetalk/venv/bin/python manage.py migrate
            ~/vibetalk/venv/bin/python manage.py collectstatic --noinput